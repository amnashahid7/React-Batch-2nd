import React from 'react';
import styles from './FooterBar.module.css'; // Importing CSS module

const FooterBar = () => {
  return (
    <div className={`${styles.footer}`} style={{textAlign:"center"}}>footerBar</div>
  )
}

export default FooterBar